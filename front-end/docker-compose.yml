version: '3'
services:
  frontend:
    build: .
    ports:
      - '3000:3000'
    networks:
      - app-network
    env_file:
      - ../.env
    depends_on:
      - backend

networks:
  app-network:
    external: true
