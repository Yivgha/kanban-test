{
  "name": "kanban-test",
  "version": "1.0.0",
  "description": "Main folder of kanban project.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10"
  },
  "lint-staged": {
    "back-end/**/*.{js,ts,mjs}": [
      "npm run lint --prefix back-end",
      "npm exec --prefix back-end prettier",
      "echo 'Back-end is great!'"
    ],
    "front-end/**/*.{js,ts,jsx,tsx}": [
      "npm run lint --prefix front-end",
      "npm exec --prefix front-end prettier",
      "echo 'Front-end is pretty!'"
    ]
  }
}
